[project]
name = "memory-game"
version = "0.1.0"
description = ""
authors = [
    {name = "Niklas Schubert",email = "niklas.schubert@thinktecture.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "memory_game", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
